# MySQL Database Connection Credentials
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DB=bavya_video_portal

# Directory configuration (set for persistent volume mount in production, e.g. /app/data)
# PERSISTENT_STORAGE_DIR=/var/data
